# Default values for mcp-atlassian
# This is a YAML-formatted file.

replicaCount: 1

image:
  repository: ghcr.io/sooperset/mcp-atlassian
  pullPolicy: IfNotPresent
  tag: "0.20.0"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Transport mode: "stdio", "sse", or "streamable-http"
# For HTTP modes, service will be created automatically
transport: "stdio"

# Port for HTTP transport modes (sse or streamable-http)
port: 9000

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

rbac:
  # Whether to create RBAC resources
  create: true

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false

service:
  type: ClusterIP
  port: 9000
  targetPort: 9000
  annotations: {}

# Server TLS (HTTPS listener)
# Serve the HTTP transports (sse/streamable-http) over HTTPS by terminating TLS
# at the MCP server itself — useful for end-to-end encryption to the pod. The
# chart does NOT create or manage certificates: provide an existing Secret (for
# example one issued by cert-manager) holding the cert and key; it is mounted
# read-only into the container and passed to the server via --ssl-certfile /
# --ssl-keyfile. The chart also sets MCP_ATLASSIAN_USE_SYSTEM_TRUSTSTORE=false,
# which the HTTPS listener requires (the OS trust store injection is
# client-side only and would reject every inbound TLS handshake). See caBundle
# below for outbound verification against a private CA. Not valid with the
# 'stdio' transport (no network listener).
# NOTE: requires an image that includes the --ssl-certfile/--ssl-keyfile
# options and the outbound CA-bundle handling; older tags crashloop on the
# unknown options. Keep image.tag/appVersion at or above the release that
# ships them.
tls:
  enabled: false
  # Name of an existing Secret (PEM cert + key). Required when tls.enabled.
  secretName: ""
  # Keys within that Secret. Defaults match a kubernetes.io/tls Secret.
  certFileKey: tls.crt
  keyFileKey: tls.key
  # Where the Secret is mounted in the container. Must NOT be under /app
  # (the container workdir), which is the confinement root for
  # caller-supplied file paths; the render enforces this.
  mountPath: /etc/mcp-atlassian/tls
  # File mode for the mounted key material (octal 0400: owner read-only).
  # Readability relies on podSecurityContext.fsGroup (kubelet group-owns the
  # projected files); if you remove fsGroup, the pod cannot read the key and
  # crashloops at the TLS preflight.
  defaultMode: 0400

# Outbound private-CA bundle (optional)
# With the OS trust store injection disabled (automatic when tls.enabled),
# outbound SSL verification uses the bundled certifi CAs. If your Atlassian
# instance is signed by a private CA, provide an existing Secret holding the CA
# bundle (PEM); it is mounted read-only and exposed via REQUESTS_CA_BUNDLE (the
# requests-based Jira/Confluence clients) and SSL_CERT_FILE (the httpx-based
# OAuth proxy paths). Include public roots in the bundle if you also talk to
# Atlassian Cloud. Setting secretName enables the mount.
caBundle:
  secretName: ""
  key: ca.crt
  mountPath: /etc/mcp-atlassian/ca

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: mcp-atlassian.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: mcp-atlassian-tls
  #    hosts:
  #      - mcp-atlassian.local

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}

# OAuth token persistence (only needed for OAuth flow with token refresh)
persistence:
  enabled: false
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 1Gi
  # existingClaim: ""

# Authentication Mode: "api-token", "personal-token", "oauth", "byot"
# (bring your own token), or "external" (trusted proxy header passthrough).
authMode: "api-token"

# Confluence Configuration
confluence:
  enabled: true
  # For Cloud: https://your-company.atlassian.net/wiki
  # For Server/DC: https://confluence.your-company.com
  url: ""

  # Cloud authentication (when authMode: api-token)
  username: ""
  apiToken: ""

  # Server/DC authentication (when authMode: personal-token)
  personalToken: ""

  # SSL verification for Server/DC (set to false for self-signed certs)
  sslVerify: "true"

  # Space filter (comma-separated space keys)
  spacesFilter: ""

  # Custom headers (comma-separated key=value pairs)
  customHeaders: ""

  # Incoming MCP request header names to pass through (comma-separated)
  passthroughHeaders: ""

# Jira Configuration
jira:
  enabled: true
  # For Cloud: https://your-company.atlassian.net
  # For Server/DC: https://jira.your-company.com
  url: ""

  # Cloud authentication (when authMode: api-token)
  username: ""
  apiToken: ""

  # Server/DC authentication (when authMode: personal-token)
  personalToken: ""

  # SSL verification for Server/DC
  sslVerify: "true"

  # Project filter (comma-separated project keys)
  projectsFilter: ""

  # Custom headers (comma-separated key=value pairs)
  customHeaders: ""

  # Incoming MCP request header names to pass through (comma-separated)
  passthroughHeaders: ""

# OAuth 2.0 Configuration (Cloud only)
# Required when authMode: oauth or byot
oauth:
  # Cloud ID (from --oauth-setup wizard or known for your instance)
  cloudId: ""

  # For standard OAuth flow (authMode: oauth)
  clientId: ""
  clientSecret: ""
  redirectUri: "http://localhost:8080/callback"
  scope: "read:jira-work write:jira-work read:confluence-content.all write:confluence-content offline_access"

  # For BYOT mode (authMode: byot)
  # Provide pre-existing access token
  accessToken: ""

# OAuth Proxy + DCR Configuration
# Optional and disabled by default. When enabled, the server exposes
# discovery + DCR routes for MCP OAuth clients.
oauthProxy:
  enabled: false

  # Optional restrictions (comma-separated values)
  allowedClientRedirectUris: ""
  allowedGrantTypes: ""

  # Require consent page during OAuth authorize flow.
  requireConsent: true

# OAuth proxy client storage backend for registered clients.
# "default": use FastMCP default encrypted storage.
# "factory": load custom storage via import path.
oauthClientStorage:
  mode: "default" # default | factory
  factory:
    importPath: "" # e.g. "my_pkg.storage:create_store"
    # Optional JSON object passed to the factory.
    configJson: ""
    # Optionally source JSON from an existing secret key.
    configJsonSecret:
      name: ""
      key: "config.json"

# Proxy Configuration
proxy:
  enabled: false
  http: ""
  https: ""
  noProxy: "localhost,127.0.0.1"
  socks: ""
  wpad:
    enabled: false
    url: ""

  # Service-specific proxy overrides
  confluence:
    http: ""
    https: ""
    noProxy: ""
    wpad:
      enabled: null # Set true/false to override the global WPAD toggle for Confluence
      url: ""
  jira:
    http: ""
    https: ""
    noProxy: ""
    wpad:
      enabled: null # Set true/false to override the global WPAD toggle for Jira
      url: ""

# Server Configuration
config:
  # Read-only mode (disables all write operations)
  readOnlyMode: false

  # Enable verbose logging
  verbose: false

  # Very verbose logging (includes debug info)
  veryVerbose: false

  # Log to stdout instead of stderr
  loggingStdout: true

  # Comma-separated list of enabled tools
  # Leave empty to enable all tools
  # Example: "confluence_search,jira_get_issue,jira_search"
  enabledTools: ""

# Additional environment variables
extraEnv: []
  # - name: CUSTOM_VAR
  #   value: "custom-value"

# Additional secrets to mount
extraSecrets: []
  # - name: custom-secret
  #   mountPath: /etc/secrets
  #   readOnly: true
